(function (global) {

    'use strict';

   // var Datastore = require('nedb');
    //global.db = new Datastore({filename: 'database', autoload: true});
    //global.db = new Datastore({filename: 'database', autoload: true});
    //global.db = new Datastore({filename: 'database', autoload: true});



}(this));